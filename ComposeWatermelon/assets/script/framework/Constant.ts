/**
 * 项目的所有枚举在该脚本下声明
 */

import { Node } from "cc";

/** 预制体实例后对应 UI 种类 */
export enum UIType {
    ControlPanel,
    StartMenu,
    LossPanel
}

export const Global_Date = {
    fruitType: 0,
    Score: 0,
    cheat: false,
};

export const WATERMELON_ARRAY: Node[] = [];

